const SERVER_PORT = 3333;
const LOCAL_TUNNEL_SUBDOMAIN = 'delhi-shubham';

// events
const MessageEvent = 'message';
const CloseEvent = 'close';

const UTF_8 = 'utf-8';

module.exports = {
    SERVER_PORT,
    LOCAL_TUNNEL_SUBDOMAIN,
    MessageEvent,
    CloseEvent
}  